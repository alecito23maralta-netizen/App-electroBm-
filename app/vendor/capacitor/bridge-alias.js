// Alias mínimo para que filesystem-plugin.js encuentre la variable global "synapse"
// que espera (viene expuesta como window.outsystemsSynapse desde synapse.js).
window.synapse = window.outsystemsSynapse;
